name: Build and Release

on:
  push:
    branches:
      - 'main'
  pull_request:
  workflow_dispatch:

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    strategy:
      matrix:
        os: [linux, windows, darwin]
        goarch: [amd64]
        include:
          - os: linux
            output_name: speedup-linux-amd64
            asset_name: speedup-linux-amd64
          - os: windows
            output_name: speedup-windows-amd64.exe
            asset_name: speedup-windows-amd64.exe
          - os: darwin
            output_name: speedup-darwin-amd64
            asset_name: speedup-darwin-amd64

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.23'

      - name: Build binary
        env:
          GOOS: ${{ matrix.os }}
          GOARCH: ${{ matrix.goarch }}
        run: go build -v -o ${{ matrix.output_name }}

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.asset_name }}
          path: ${{ matrix.output_name }}

  release:
    name: Create Release
    if: github.event_name != 'pull_request'
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Delete the existing pre-release
        run: gh release delete dev-build --cleanup-tag --yes --repo $GITHUB_REPOSITORY
        env:
          GITHUB_TOKEN: ${{github.token}}

      - name: Create new release
        uses: ncipollo/release-action@v1
        with:
          allowUpdates: true
          artifactErrorsFailBuild: true
          artifacts: "artifacts/*/*"
          body: |
            **自动构建版本**
            构建时间: ${{ github.event.head_commit.timestamp }}
            提交信息: ${{ github.event.head_commit.message }}
            提交SHA: ${{ github.sha }}
          name: Development build
          prerelease: true
          removeArtifacts: true
          tag: dev-build